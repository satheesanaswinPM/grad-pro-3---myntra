datasets>=2.20.0
huggingface_hub>=0.24.0
apify-client>=1.8.0
google-play-scraper>=1.2.0
pyarrow>=15.0.0
streamlit>=1.32.0


# Phase 0 inspect uses the standard library only.
# Optional readers detected at runtime:
#   pyarrow   — .parquet
#   openpyxl  — .xlsx
